{
  "name": "eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "format": "biome format . --write",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "run": "bun run runner.ts",
    "report": "bun run reporter.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@types/bun": "latest"
  }
}
